// core/router.js — placeholder para SPA por hash (expansível futuramente)
export function initRouter(){
  // Neste projeto, a "SPA" básica é aplicada na página de projetos via hash (#/projetos/:categoria).
  // Mantemos este módulo para futura expansão se necessário.
}
